# 阶段1：Python 基础

> 优先级：★★★★★（最高）
> 预计时间：4-6 周
> 状态：学习中
> 已学课时：1.1 ~ 1.5 + 周练习（第1周完成 ✅）、2.1 ~ 2.6 + 周练习（第2周完成 ✅）、3.1 ~ 3.7 + 周练习（第3周完成 ✅）、文件操作速成（5.1 预热：open/with/read/write）✅、4.1 ✅、4.2 ✅、4.3 ✅、4.4 ✅、4.5 ✅
> 上次学习：2026-09-16（4.5 魔术方法**验收通过**：lesson5.py 导师全量验证全绿；同日提交模式经两轮协商定终版——代码+运行结果+思考题，自查仪式项取消，导师全量验证+缺陷归因条款，见 CLAUDE.md；第 4 周余周练习）
> 🔖 当前进度指针：阶段1 进度 **26/33**，第 4 周进行中：4.1 ✅（lesson1.py·TestCase 类）、4.2 ✅（2026-09-14，练习/week4/lesson2.py·TestResult 类：类属性计数器+静态谓词校验+classmethod 工厂 from_csv_line+get_total_count；3 轮明细：R1 四件套齐、输出全对，但 totle_count typo（**第 8 次，一致性形态复发**，间隔 7 天重演 pass_rete）/is_valid_status 违反谓词契约（raise+永真，规格要 bool）/get_total_count 定义未用（**假阳性新形态：len(examples) 恰等 5 掩盖机制未测**）/开课 gitignore 任务被跳过；R2 三项修复+**map 直传方法引用**（非阻塞提示被采纳）+grep 证据交了，但校验顺序只改一半（**第 6 次**）、git 部分**诚实申报未做（零假勾，对比早上的类名假勾，同日显著进步）**；R3 文件检查模式——导师实测声明为真（校验前置+边界回归+计数器防污染全绿）放行，**记录在案：下次重修无证据直接打回**。插曲：week4/main.py 查实为 PyCharm 自动示例垃圾→删除，.gitignore 乱码死规则行→清除，立原则「垃圾删除不供奉，ignore 只收必须存在但不该入库的（产物/.venv/.idea）」；学生主动说明 main.py 来历（沟通上下文改变解法，表扬）。思考题 3/3 高质量：self 机制对、cls 继承方向猜对（**伏笔已在 4.3 回收 🌟**）、命名空间归属答到本质。**4.3 ✅ 已验收**（2026-09-15，练习/week4/lesson3.py·迷你用例管理系统：BaseCase 类属性计数器（类名正确递增，避开 self.xxx+=1 坑）+ ApiCase/UICase 单继承覆盖 run() + super().__init__ 委托 + isinstance/issubclass；4 次提交：S1 仪式缺 3 项（运行命令/无产物声明/核对声明只写"已核实"两字）一行打回——零容忍第 2 次执行；S2（评审 R1）仪式齐且**功能一次全绿——第 4 周首次 R1 全绿**（存盘与贴文逐字符一致、主流程 7 行逐字符、import 无副作用防护第 2 次实证、边界 4/4：status_code 199/200/201、element_found True/False、父类 NOT_RUN、计数 6→7、isinstance/issubclass），但 PEP 8 两处：**44 行冗余 f 前缀复发（错题本第 6 条惯犯）** + **48 行注释 totaL_count（typo 家族第 9 次，新形态：注释 typo 不报错）**→修复清单 F1/F2；S3 修复齐但缺对照表/其余行未动声明/实验情况→一行打回，零容忍第 3 次执行；S4 三项补齐（行号证据 44/48+声明+报错原文 'ApiCase' object has no attribute 'case_id' 贴出）→导师 Read 逐行核对仅两处预期差异、回归 7 行全对，**通过**。思考题 4/4 方向全对：题1 self.total_count+=1 假计数器结果全对（类属性 0/实例各 1，WrongCounter 实测印证，补口诀"读走继承链，self.xxx= 写落实例"）；题2 浅→补"决定跑哪个 run 的不是代码写在哪个类而是 self 是谁，查找从实例的类开始"——**4.4 多态地基已埋**；题3 推演+实验全对（彩蛋已授：不调 super 连计数也不加）；题4 cls=发起调用的类。亮点：功能 R1 全绿（第 4 周首次）、UICase.run 真值判断 Pythonic、实验报告贴报错原文。纪律观察：仪式项两次"首交不齐、打回后齐"（对比第 3 周连缺 3 轮有进步，但标准是**一次交齐**）。非阻塞提示：三个类均缺 docstring（第 3 周曾表扬该习惯，防退化→4.4 已升级为硬性要求并一次落实 ✓）。**4.4 ✅ 已验收**（2026-09-15，练习/week4/lesson4.py·多态用例执行器：ApiCase/UICase 各跑各的 run() + Report 封装 __records（add/summary）+ batch_run 统一入口禁 isinstance；S1 缺运行命令一行打回（零容忍第 4 次执行），S2 仪式齐+存盘与贴文逐字符一致（含思考题注释）→**功能 R1 全绿连续第 2 次** 🌟；导师证据链：grep 无 isinstance/无 type(/无 f 前缀无占位符 ✓、主流程 5 行逐字符 ✓、边界 6/6（空 Report'总数 0，PASS 0，FAIL 0'、batch_run 空列表、UICase 元素 ''/#btn 两侧、类外直接 __records AttributeError 且报错原文与学生声称逐字符一致、名称改写 _Report__records 拿到、鸭子类型 PerfCase 混跑'总数 2，PASS 1，FAIL 1'）✓、import 无副作用第 3 次实证 ✓；PEP 8 非阻塞 2 处：docstring 三行式（建议 PEP 257 单行式）、UICase.run 的 return 后冗余 else。思考题 3/3：题1'是哪个类的实例就跑哪个的 run()'方向对但漏'什么时候'（已补：case.run() 调用那一刻，查找从实例的类开始向上）；题2 实验声称报错原文与导师实测一致（真实非编造）；题3 自发说出'无需修改原有代码，降低引入 bug 风险'=开闭原则精髓 🌟。纪律观察：仪式项再次首交不齐（缺运行命令，打回后齐——模式延续，标准是一次交齐）；亮点：docstring 硬性要求一次落实、typo 零、冗余 f 前缀零（惯犯未复发）。**4.5 ✅ 已验收**（2026-09-16，练习/week4/lesson5.py·TestCase __str__/__repr__/__eq__（只比 case_id）+ CaseSuite __len__/__str__；S1 **仪式 5 件套首次一次交齐 🌟（零容忍执行 4 次后的里程碑）**、存盘与贴文逐字符一致、主流程 8 行逐字符 ✓、边界 10/10（import 无副作用第 4 次实证、默认状态 NOT_RUN、__eq__ 自反 True/不同编号 False/非 TestCase False、空套件 len 0+str 正常、B4 证实 print 回退 __repr__、B5 证实 __len__ 返字符串 TypeError）✓；思考题 4 实验后**现场已还原**（存盘 __len__ 完好，第 3 周错题第 3 条未复发）🌟、报错原文与导师 B5 实测逐字符一致（真实非编造）；思考题批改：1✓（回退方向结论导师已补全）、2 半对（'实例都指向True'表述不清——漏核心'__eq__ 只比 case_id，status 根本没进比较'；场景'断言的时候'过泛，已补回归对比/断言含状态场景）、3 方向对但含糊（已补：print(列表) 属开发者调试视角故用元素 __repr__）、4✓；**修复清单 F1**=34 行 self.__len__() 直接调用魔术方法→改 len(self)（违反本课核心'魔术方法由语法触发，不由手调用'）、**F2**=14 行 __repr__ 手写引号 '{self.case_id}'→{self.case_id!r}（题目提示明确指向 !r，手写引号遇值内含引号即错格式）、**F3**=19-20 行 return 后冗余 else（4.4 非阻塞提示后第 2 次复发→升级必修）；非阻塞：9/13 行 def 后多余空行（可采纳，采纳须列入对照表）。插曲：S2 首交缺 4 项零容忍第 5 次打回→学生情绪反弹「要求太苛刻浪费时间」→导师承认修复轮仪式成本失衡、给两条路（照模板收尾/正式改规则）→学生提议「修改后复核没问题就行」→协商出**修复轮轻量三件套**新规（当日生效写入 CLAUDE.md）。S2 重交：学生拒执行三件套并宣告「只贴代码和运行结果，后续都是如此」→导师重算成本收益（对照表/声明的验证价值可由导师读盘+回归全额替代，仅剩训练价值而训练需本人配合，强扭无益）→**提交模式第三次修订·终版**（已写入 CLAUDE.md）：每交=代码+运行结果+思考题；自查仪式项全部取消；导师全量验证；缺陷归因条款（自查可拦截的缺陷打回时错题本归因「未自查」）；第 4 周收官数据复盘定去留；思考题为底线不可谈（事实上学生始终自愿提交思考题）。4.5 S2 按新模式验证**通过**：导师读盘=贴文逐字符 ✓、F1 31 行 len(self) ✓ / F2 12 行 !r ✓ / F3 14-17 行 else 已删 ✓ / 9、13 行空行已删 ✓、主流程 8 行逐字符 ✓、边界回归 9/9 ✓（import 无副作用第 4 次实证/默认 NOT_RUN/__eq__ 自反·不同编号·非 TestCase/空套件 0/print 回退 __repr__/BadLen TypeError）、grep 冗余 f 前缀·else·__len__() 零命中 ✓ → **4.5 ✅**。下一站：**周练习：用 OOP 重构学生成绩管理系统【未布置】**——用 4.1~4.5 全部工具重构第 2 周成绩管理系统（招银 JD 点名面向对象，不走过场）；通过后写第 4 周总结与错题本（须含仪式争议全记录与数据复盘）。4.1 验收明细（4 轮）：R1 缺终端输出+声明——零容忍规则第 1 次执行（一行打回不评审）；R2 输出全对，**学生实跑对照预期自己抓到 `x.is_p0` 缺调用括号**（方法引用永真→filter 全留→3≠2，假阳性类 bug 首次被学生自查抓住，教学闭环 🌟）；但类名 Tets_Case（题目规格 TestCase+PEP8 大驼峰+typo 三重）打回；R3 只删下划线成 TetsCase、typo 依旧——**对照表假勾第 2 次 + 改清单只改一半第 5 次**（自称"类名已修改"与磁盘不符），打回并教 `grep -n` 验证法；R4 学生自己在终端跑 grep 贴磁盘证据（1/17/18 行 4 处命中）→ 通过，**首次亲手完成"声明→证据"闭环** 🌟。grep 输出中文乱码（终端 GBK 解码 UTF-8）已解释为显示层问题、文件完好（导师实跑中文正常），编码家族留待阶段3。思考题批改：self"谁传的"漏答已补（Python 自动传）；AttributeError 实验做了但没读报错里的类名提示（已点）；对象好处答对 2 条（补第 3 条：具名属性消灭魔法下标）。第 4 周总结引用——错题：typo 存活 3 轮（Tets，Python 报错喊过+打回写过原文）、假勾 2 次、改一半 5 次、仪式缺项 R1 一次；亮点：is_p0() 自查抓获、grep 证据首用、filter+lambda 正迁移、口试 3/3。历史存档：第 3 周总结在 `笔记/2026-09-13_第3周总结与错题本.md`；补档已闭环（豁免仅此次）；09-13 git 事故（.gitignore GBK 乱码死规则）已修复，教训"规则写了≠生效，git check-ignore -v 验证"。📎 补档记录（已闭环）：R5「文件检查模式」缺学生自跑输出与声明，原定下次开场补档，09-13 学生申请豁免（理由：导师已全绿验证），导师接受并记录——**豁免仅此一次，第 4 周起 5 件套+清单对照表零容忍**。导师验证事实：主流程逐字符一致 ✓ / 边界 4/4（空列表 ValueError·全PASS 100.0%·全FAIL 0.0%·空文件端到端）✓ / 产物首行+尾换行 ✓ / import 无副作用 ✓（思考题1活证明）。📕 同日 git 事故（未核实类，与假阳性同源）：学生在 cmd/PowerShell 用 echo>> 向 UTF-8 的 .gitignore 追加中文路径 → GBK 乱码+字面引号 = 两条死规则，产物 4 文件随之入库；且学生在死规则未验证的情况下声称"文件都检查过没有问题"——导师以 git check-ignore -v 抓包并重写修复。教训固化：**规则写了 ≠ 规则生效（git check-ignore -v 验证）；Windows 控制台 echo>> 会污染 UTF-8 文件编码，追加配置用编辑器**。week3 已入库产物留作验收证据不撤，.gitignore 新模式管未来（*.log + 练习/**/report|results|*_report.txt）
> 📌 欠账：无阻塞项。第 4 周纪律与教学提醒：① 提交仪式 5 件套（代码/完整终端输出**含运行命令**/产物文件内容或"无产物文件"声明/两项核对声明**分项写明**/思考题）+ 打回重修附清单对照表（每条 ✓+行号证据）+"其余行未动"声明——**缺任一项零容忍**：一行回复打回、不评审（历史：第 3 周周练连缺 3 轮+R4 对照表假勾 1 次；4.3 同课时零容忍执行 2 次——首交缺仪式 3 项、修复交缺证据 3 项；4.4 S1 缺运行命令=零容忍第 4 次，打回后齐；**4.5 S1 首次一次交齐 🌟 达标**；4.5 S2 首交缺 4 项→零容忍第 5 次打回→学生 pushback「重复提交太繁琐」→**2026-09-16 规则修订当日生效：修复轮轻量三件套**（免重贴全文；对照表一行含修复后新行号+「其余行未动」声明+回归命令与输出；新课时首交仍 5 件套；已同步 CLAUDE.md）；同日学生二次 pushback「后续只交代码和运行结果」→导师终局立场：核对类声明合并为一行（让步），**思考题/运行命令/修复轮对照表/其余行未动四项不让**（各对应实证缺陷：思考题=学习本体、命令连漏 2 轮、只改一半 5 次+假勾 1 次、回归事故 1 次），标准宣布终版；学生再拒执行（「只贴代码和运行结果，后续都是如此」）→**第三次修订·终版**：每交=代码+运行结果+思考题，自查仪式项全取消，导师全量验证+缺陷归因条款（自查可拦截缺陷错题本归因「未自查」），第 4 周收官数据复盘定去留，思考题/底线不让（已同步 CLAUDE.md））；② 开课前提问先过总结错题本高频项：假阳性 6 次（核对产物不只核对回显）、改清单不全 4 次、遮蔽 4 次（sum/dict/summarize/id）、**typo 9 次（4.3 新形态：注释 typo totaL_count）**、**冗余 f 前缀复发（错题本第 6 条惯犯，4.3 S2 又犯）**、回归事故 1 次；③ 亮点继续养：**功能 R1 全绿连续 3 次（4.3/4.4/4.5）**、**4.5 S1 仪式五件套首次一次交齐**、docstring 硬性要求一次落实、typo 零/冗余 f 零（惯犯未复发）、思考题自发说出开闭原则、思考题 4 实验后现场还原、实验报告贴报错原文、技术 pushback（4.5 升级为规则协商并促成验收模式改版——姿势正当，注意协商频次与第 4 周数据复盘之约）、ValueError 防御自发迁移、数字逻辑连续 5 轮一次全对
> ⏭️ 之后：第 4 周 OOP（4.1✅ → 4.2✅ → 4.3✅ → 4.4✅ → 4.5 魔术方法 → 周练习：用 OOP 重构学生成绩管理系统）。**特别注意**：招银测开 JD（12-24K·14薪）点名"精通面向对象系统架构设计"，是金融IT 主线的技术底座，不走过场（2026-09-12 JD 数据反哺，全局调整见 测试开发/学习计划.md「JD 数据反哺调整」节）。5.1 正课只需补 CSV/JSON 与综合读写（文本读写已由速成覆盖，勿重复讲）

---

## 📚 课程大纲

### 第1周：Python 入门

| 课时 | 内容 | 状态 |
|------|------|------|
| 1.1 | Python 安装与环境配置（PyCharm/VSCode） | ✅ |
| 1.2 | 变量与数据类型（int, float, str, bool） | ✅ |
| 1.3 | 运算符（算术、比较、逻辑、赋值） | ✅ |
| 1.4 | 字符串操作（切片、格式化、常用方法） | ✅ |
| 1.5 | 输入输出（input, print, f-string） | ✅ |
| 练习 | 写一个简易计算器 | ✅ |

### 第2周：流程控制与数据结构

| 课时 | 内容 | 状态 |
|------|------|------|
| 2.1 | 条件判断（if/elif/else） | ✅ |
| 2.2 | 循环（for, while, break, continue） | ✅ |
| 2.3 | 列表（list）— 增删改查、切片、推导式 | ✅ |
| 2.4 | 元组（tuple）与集合（set） | ✅ |
| 2.5 | 字典（dict）— 增删改查、遍历 | ✅ |
| 2.6 | 数据结构综合练习 | ✅ |
| 练习 | 学生成绩管理系统（命令行版） | ✅ |

### 第3周：函数与模块

| 课时 | 内容 | 状态 |
|------|------|------|
| 3.1 | 函数定义与调用 | ✅ |
| 3.2 | 参数（位置参数、关键字参数、默认值、*args、**kwargs） | ✅ |
| 3.3 | 返回值（单值、多值） | ✅ |
| 3.4 | 作用域（局部变量、全局变量） | ✅ |
| 3.5 | Lambda 表达式 | ✅ |
| 3.6 | 模块与包（import、from...import、__name__） | ✅ |
| 3.7 | 常用内置函数（map, filter, sorted, enumerate, zip） | ✅ |
| 练习 | 封装一个文件处理工具模块（测试结果文件处理工具模块，5 轮验收通过） | ✅ |

### 第4周：面向对象编程

| 课时 | 内容 | 状态 |
|------|------|------|
| 4.1 | 类与对象（class、__init__、self） | ✅ |
| 4.2 | 属性与方法（实例属性、类属性、实例方法、类方法、静态方法） | ✅ |
| 4.3 | 继承（单继承、多继承、super()） | ✅ |
| 4.4 | 多态与封装 | ✅ |
| 4.5 | 魔术方法（__str__, __repr__, __len__, __eq__） | ✅ |
| 练习 | 用面向对象重构学生管理系统 | ⬜ |

### 第5周：Python 进阶

| 课时 | 内容 | 状态 |
|------|------|------|
| 5.1 | 文件操作（读写、with语句、CSV/JSON处理） | ⬜ |
| 5.2 | 异常处理（try/except/finally、自定义异常） | ⬜ |
| 5.3 | 装饰器（基础装饰器、带参装饰器） | ⬜ |
| 5.4 | 迭代器与生成器 | ⬜ |
| 5.5 | 正则表达式（re模块） | ⬜ |
| 5.6 | 日期时间处理（datetime、time） | ⬜ |
| 练习 | 日志分析脚本（读取日志文件，提取关键信息） | ⬜ |

### 第6周：Python 实战（测试相关）

| 课时 | 内容 | 状态 |
|------|------|------|
| 6.1 | os/pathlib — 文件和目录操作 | ⬜ |
| 6.2 | json/yaml — 配置文件解析 | ⬜ |
| 6.3 | requests 入门 — HTTP 请求基础 | ⬜ |
| 6.4 | logging — 日志模块 | ⬜ |
| 6.5 | unittest — Python 内置测试框架 | ⬜ |
| 6.6 | 虚拟环境（venv/pip/requirements.txt） | ⬜ |
| 综合项目 | 接口数据校验工具（读取Excel数据，请求接口，校验返回值） | ⬜ |

---

## 📝 学习笔记
> 学习过程中在「笔记/」目录下记录重点

## 💻 练习代码
> 练习代码放在「练习/」目录下

## 🔄 复习记录
> 每周末在此记录复习情况
